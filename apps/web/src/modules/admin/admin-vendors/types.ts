/** Vendor list item: profile + phone from joined user */
export type VendorListItem = {
  id: string;
  phoneNumber: string | null;
  shopName: string;
  city: string;
  marketHub: string;
  isActive: boolean;
};
