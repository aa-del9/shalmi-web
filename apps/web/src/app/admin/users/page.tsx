import { ComingSoonShell } from '@/modules/admin/admin-dashboard/components/coming-soon';

export default function AdminUsersPage() {
  return (
    <ComingSoonShell
      title="Customers"
      trail={['Operations', 'Customers']}
      description="A buyer-side customer directory lands in a follow-up milestone."
    />
  );
}
